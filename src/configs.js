const path = require('path')

const PREFIX = '@'
const BOT_EMOJI = '🍁'
const TEMP_FOLDER = path.resolve(__dirname, '..','assets','temp')

module.exports = {
    TEMP_FOLDER,
    PREFIX,
    BOT_EMOJI
}